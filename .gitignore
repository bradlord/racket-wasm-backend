# The cloned upstream tree and all build artifacts live here (disposable;
# regenerated by `racket build/main.rkt sync apply`).
/.work/

# Collected build outputs.
/dist/

# Editor / OS noise.
*.sw?
.DS_Store
/compiled/
**/compiled/

diff --git a/racket/src/cs/c/build.zuo b/racket/src/cs/c/build.zuo
index b4e92ee..fb294f4 100644
--- a/racket/src/cs/c/build.zuo
+++ b/racket/src/cs/c/build.zuo
@@ -54,6 +54,35 @@
   (define boot-files (list petite-chunked.boot scheme-chunked.boot racket-chunked.boot))
 
   (define cross? (equal? (lookup 'CROSS_MODE) "cross"))
+  ;; A WebAssembly/Emscripten target: the native `racketcs` executable
+  ;; and the `embed-boot` step are not meaningful (boot images are
+  ;; preloaded into MEMFS at the emcc link, not embedded). The CS build
+  ;; stops at the (pbchunk) boot images; the `wasm` link target consumes
+  ;; them together with the in-tree kernel/rktio objects.
+  (define emscripten? (equal? (lookup 'EMSCRIPTEN) "t"))
+  ;; Cross-compiler SDK build (main.zuo `sdk?` flavor, signalled via WASM_SDK):
+  ;; build only the cross-compiler inputs (boot/xpatch + the host CS-layer
+  ;; objects) and run the cross `raco setup`, but DON'T build the Chez C kernel
+  ;; -- it is a runtime/link input the SDK never ships, and (unlike the host
+  ;; cross-compiler pieces) its libffi headers come from the emsdk `wasm-deps`
+  ;; step, which the SDK skips. See build-wasm.md "Cross-compiler SDK".
+  (define sdk? (equal? (lookup 'WASM_SDK "") "t"))
+  ;; WASM native library deps. libffi is always built (the Chez kernel's
+  ;; ffi.c needs its headers); `WASM_DEPS` selects additional libraries
+  ;; (recipe names, or the group alias `draw`). The `wasm-deps` target
+  ;; runs the recipe driver, which writes three artifacts into
+  ;; `wasm-deps-dir`: wasm_deps.inc (symbol registrations, #include'd by
+  ;; wasm_extras.inc at the boot.o compile), wasm_deps_uflags.txt
+  ;; (-Wl,-u force-link flags), and .wasm-deps-linkflags.txt (the -L/-l
+  ;; flags), both consumed by the `wasm` link below.
+  (define wasm-deps-spec (lookup 'WASM_DEPS ""))
+  (define wasm-deps-dir (at-dir "wasm-deps"))
+  ;; The recipe sources (build-deps.sh + deps/) live outside the tree, in
+  ;; the racket-wasm orchestrator's wasm-deps/ dir, passed via the
+  ;; WASM_DEPS_SRC_DIR make var. Only the OUTPUT dir (`wasm-deps-dir`) is
+  ;; in the build tree. Validated lazily in the `wasm-deps` target.
+  (define wasm-deps-src-dir (lookup 'WASM_DEPS_SRC_DIR ""))
+  (define wasm-deps.inc (build-path wasm-deps-dir "wasm_deps.inc"))
 
   (define scheme.exe+args
     (cond
@@ -115,7 +144,26 @@
                'enablelibffi (lookup 'SCHEME_LIBFFI "no")
                'disablecurses (lookup 'DISABLE_CURSES "no")
                'disableiconv (lookup 'DISABLE_ICONV "no")
-               'exePostStep (lookup 'POST_LINKER)))
+               'exePostStep (lookup 'POST_LINKER)
+               ;; The Chez kernel compile (c/ffi.c) needs ffi.h, and the
+               ;; scheme-exe link needs libffi (ffi.o references
+               ;; ffi_prep_cif / ffi_type_*). The wasm libffi isn't in a
+               ;; system include/lib dir, so point at the headers and
+               ;; archive built by the `wasm-deps` target (the recipe
+               ;; driver always builds libffi first, before the kernel).
+               ;; `-lffi` goes in LIBS so it follows libkernel.a on the
+               ;; link line.
+               'CPPFLAGS (if emscripten?
+                             (build-shell
+                              (hash-ref config 'CPPFLAGS "")
+                              (~a "-I" (at-source "../../build-libffi-em/install/include")))
+                             (hash-ref config 'CPPFLAGS ""))
+               'LIBS (if emscripten?
+                         (build-shell
+                          (hash-ref config 'LIBS "")
+                          (~a "-L" (at-source "../../build-libffi-em/install/lib"))
+                          "-lffi")
+                         (hash-ref config 'LIBS ""))))
 
   (define (add-scheme-boot-config config)
     (if (equal? (lookup 'ENABLE_EXTRA_CHECKS "no") "yes")
@@ -174,6 +222,25 @@
     (map .c->.o pbchunk-srcs))
 
   (define boot.o (at-dir (.c->.o "boot.c")))
+  ;; WASM/Emscripten link inputs (see the `wasm` target): Racket's
+  ;; Emscripten entry point and the rktio-init object, compiled with emcc.
+  (define main_em.o (at-dir "main_em.o"))
+  (define init_rktio.o (at-dir "init_rktio.o"))
+  ;; WASM-specific C primitives: the http/canvas/dom entry points that
+  ;; wasm_extras.inc registers via Sforeign_symbol (so boot.o references
+  ;; them), plus wasm_stubs.c, which stubs the libc functions the recipe
+  ;; deps reference but Emscripten lacks (getprogname, copy_file_range,
+  ;; res_query, pthread_setname_np, ...). All four are linked into the
+  ;; `wasm` target. Standard-header-only, so they compile with the base
+  ;; config + the PORTABLE_BYTECODE/zlib defines.
+  (define wasm-prim-names (list "wasm_http" "wasm_canvas" "wasm_dom" "wasm_stubs"))
+  (define wasm-prim-srcs (map (lambda (n) (at-source (~a n ".c"))) wasm-prim-names))
+  (define wasm-prim-objs (map (lambda (n) (at-dir (~a n ".o"))) wasm-prim-names))
+  ;; Browser-only primitive: wasm_shell_io.c backs the SAB-based stdin/stdout
+  ;; the browser shell worker drives. It is linked into the browser surface
+  ;; (`scheme-web.*`) only, never the node surface (`scheme.*`).
+  (define wasm_shell_io.src (at-source "wasm_shell_io.c"))
+  (define wasm_shell_io.o (at-dir "wasm_shell_io.o"))
   (define boot-objs
     (append (list boot.o)
             (list (at-dir CS m "boot" m (.a "kernel")))
@@ -407,10 +474,14 @@
      `([:target build ()
                 ,(lambda (token)
                    (build/dep (find-target "build-racketcs" the-targets) token)
-                   (build/dep (find-target gracketcs the-targets) token)
-                   (build/dep (find-target "starter" starter-targets) token)
-                   (when (and mac? (not mac-fw?))
-                     (build/dep (find-target framework the-targets) token)))
+                   ;; For a WASM target there is no native gracket/starter
+                   ;; executable and no Mac framework -- the build stops at
+                   ;; the boot images produced by `build-racketcs`.
+                   (unless emscripten?
+                     (build/dep (find-target gracketcs the-targets) token)
+                     (build/dep (find-target "starter" starter-targets) token)
+                     (when (and mac? (not mac-fw?))
+                       (build/dep (find-target framework the-targets) token))))
                 :recur]
 
        [:target build-and-prepare ()
@@ -444,10 +515,21 @@
                                                   'vars (hash 'BOOTFILE_RACKET (lookup 'RACKET)))))
                         (build (find-target "scheme" host-targets-at) token)]))
                    (build/dep (find-target "bootfiles" the-targets) token)
-                   (build/dep (find-target "scheme" the-targets) token)
+                   ;; `scheme` builds (for a cross target) the Chez C kernel,
+                   ;; whose libffi headers come from the emsdk `wasm-deps` step.
+                   ;; The SDK ships no kernel, so skip it -- the host CS-layer
+                   ;; objects + boot/xpatch below need only the host scheme.
+                   (unless sdk?
+                     (build/dep (find-target "scheme" the-targets) token))
                    (unless cross?
                      (build/dep (find-target racket.boot the-targets) token))
-                   (build/dep (find-target racketcs the-targets) token))
+                   (if emscripten?
+                       ;; Stop at the (pbchunk) boot images; building
+                       ;; `racket-chunked.boot` transitively builds
+                       ;; `racket.boot` and the pbchunk C sources. The
+                       ;; native `racketcs`/embed-boot step is skipped.
+                       (build/dep (find-target racket-chunked.boot the-targets) token)
+                       (build/dep (find-target racketcs the-targets) token)))
                 :recur]
 
        ,(if windows?
@@ -672,13 +754,31 @@
                          ,(at-source "../../ChezScheme/c/self-exe.c")
                          ,(at-source "path_replace.inc")
                          ,(at-source "boot.h")
-                         ,rktio_config.h)
+                         ,rktio_config.h
+                         ;; For Emscripten, boot.c includes wasm_extras.inc
+                         ;; (which #includes the generated wasm_deps.inc),
+                         ;; so rebuild when the registered symbol set changes.
+                         ,@(if emscripten?
+                               (list (at-source "wasm_extras.inc") wasm-deps.inc)
+                               '()))
                 ,(lambda (boot.o token)
                    (c-compile boot.o
                               (at-source "boot.c")
                               (let* ([config (if pbchunk?
                                                  (config-merge config 'CPPFLAGS "-DPBCHUNK_REGISTER")
                                                  config)]
+                                     ;; Register the wasm dep / libm foreign
+                                     ;; symbols: compile boot.c's init_foreign
+                                     ;; with the extra-foreign include, and put
+                                     ;; wasm_extras.inc (here) + wasm_deps.inc
+                                     ;; (the deps dir) on the include path.
+                                     [config (if emscripten?
+                                                 (config-include
+                                                  (config-merge config 'CPPFLAGS
+                                                                "-DRACKET_EXTRA_FOREIGN_INC='\"wasm_extras.inc\"'")
+                                                  (at-source ".")
+                                                  wasm-deps-dir)
+                                                 config)]
                                      [config (config-include config
                                                              (at-dir CS m "boot" m)
                                                              (at-source "../../rktio")
@@ -691,6 +791,289 @@
                               (at-source "../../start/MemoryModule.c")
                               config))]
 
+       ;; --- WASM/Emscripten link (the `wasm` target below) ---------------
+       ;; Racket's Emscripten entry point, compiled with emcc (CC).
+       [:target ,main_em.o (,(at-source "main_em.c")
+                            ,(at-source "boot.h")
+                            ,(at-dir CS m "boot" m "scheme.h"))
+                ,(lambda (path token)
+                   (c-compile main_em.o
+                              (at-source "main_em.c")
+                              (config-merge
+                               (config-include config
+                                               (at-dir CS m "boot" m)
+                                               (at-dir CS m "c")
+                                               (at-source "../../ChezScheme/c"))
+                               'CPPFLAGS "-DPORTABLE_BYTECODE -sUSE_ZLIB=1")))]
+
+       ;; The rktio-init object (registers rktio's foreign symbols).
+       [:target ,init_rktio.o (,(at-source "../../ChezScheme/c/init_rktio.c")
+                               ,(at-source "../../ChezScheme/c/init_rktio.h")
+                               ,rktio_config.h)
+                ,(lambda (path token)
+                   (c-compile init_rktio.o
+                              (at-source "../../ChezScheme/c/init_rktio.c")
+                              (config-merge
+                               (config-include config
+                                               (at-source "../../rktio")
+                                               rktio-dir
+                                               (at-dir CS m "boot" m)
+                                               (at-dir CS m "c"))
+                               'CPPFLAGS "-DPORTABLE_BYTECODE -sUSE_ZLIB=1")))]
+
+       ;; WASM C primitives (wasm_http/canvas/dom/stubs.o). One target per
+       ;; source; standard headers only, so the base config plus the
+       ;; PORTABLE_BYTECODE/zlib defines suffices (CC=emcc and -pthread come
+       ;; from the emscripten configure).
+       ,@(map (lambda (src obj)
+                `[:target ,obj (,src)
+                  ,(lambda (path token)
+                     (c-compile obj src
+                                (config-merge config 'CPPFLAGS
+                                              "-DPORTABLE_BYTECODE -sUSE_ZLIB=1")))])
+              wasm-prim-srcs wasm-prim-objs)
+
+       ;; Browser-only primitive object (see wasm_shell_io.o above). Same
+       ;; compile rule as the wasm-prim objects; linked only into scheme-web.
+       [:target ,wasm_shell_io.o (,wasm_shell_io.src)
+                ,(lambda (path token)
+                   (c-compile wasm_shell_io.o wasm_shell_io.src
+                              (config-merge config 'CPPFLAGS
+                                            "-DPORTABLE_BYTECODE -sUSE_ZLIB=1")))]
+
+       ;; Build the recipe-driven native library deps (libffi always;
+       ;; more via WASM_DEPS). Writes wasm_deps.inc / wasm_deps_uflags.txt
+       ;; / .wasm-deps-linkflags.txt into wasm-deps-dir, consumed by the
+       ;; boot.o compile and the `wasm` link. The driver is incremental
+       ;; (per-dep state cache), so re-running is cheap. main.zuo builds
+       ;; this before the kernel `build`, since ffi.c needs libffi's headers.
+       [:target wasm-deps ()
+                ,(lambda (token)
+                   (unless emscripten?
+                     (error "wasm-deps target requires an Emscripten cross build (configure with --host=...-emscripten)"))
+                   (when (equal? wasm-deps-src-dir "")
+                     (error (~a "wasm-deps: WASM_DEPS_SRC_DIR is not set;"
+                                " the racket-wasm orchestrator sets it --"
+                                " for a manual `make wasm` pass"
+                                " WASM_DEPS_SRC_DIR=<racket-wasm>/wasm-deps")))
+                   (mkdir-p wasm-deps-dir)
+                   (shell/wait
+                    (map string->shell
+                         (list (build-path wasm-deps-src-dir "build-deps.sh")
+                               "--src" (at-source "../..")
+                               "--out" wasm-deps-dir
+                               "--deps" wasm-deps-spec))))
+                :recur]
+
+       ;; Final emcc link: Racket-on-WASM for node. Consumes the in-tree
+       ;; kernel/rktio/boot/pbchunk artifacts produced by `bin/zuo . build`
+       ;; plus the recipe deps from the `wasm-deps` target (libffi + any
+       ;; WASM_DEPS): their -L/-l flags and -Wl,-u force-link flags.
+       ;; Run from the build dir: `bin/zuo . wasm`.
+       [:target wasm ()
+                ,(lambda (token)
+                   (unless emscripten?
+                     (error "wasm target requires an Emscripten cross build (configure with --host=...-emscripten)"))
+                   (unless pbchunk?
+                     (error "wasm target expects PBCHUNK_MODE=pbchunk (configure with --enable-pb)"))
+                   ;; Build the recipe deps (libffi + WASM_DEPS) and the
+                   ;; cheap/wasm-specific link inputs.
+                   (build/dep (find-target "wasm-deps" the-targets) token)
+                   (build/dep (find-target main_em.o the-targets) token)
+                   (build/dep (find-target init_rktio.o the-targets) token)
+                   (for-each (lambda (o) (build/dep (find-target o the-targets) token)) wasm-prim-objs)
+                   ;; Browser-only object (linked into scheme-web only).
+                   (build/dep (find-target wasm_shell_io.o the-targets) token)
+                   (build/dep (find-target boot.o the-targets) token)
+                   (build/dep librktio.a token)
+                   (for-each (lambda (o) (build/dep (find-target o the-targets) token)) pbchunk-objs)
+                   (for-each (lambda (b) (build/dep (find-target b the-targets) token)) boot-files)
+                   ;; The kernel + lz4 come from `bin/zuo . build`; require them.
+                   (define libkernel.a (at-dir CS m "boot" m (.a "kernel")))
+                   (define liblz4.a (at-dir CS m "lz4/lib/liblz4.a"))
+                   (for-each (lambda (f)
+                               (unless (file-exists? f)
+                                 (error (~a "missing prerequisite: " f
+                                            " -- run `bin/zuo . build` first"))))
+                             (list libkernel.a liblz4.a))
+                   (define emcc (lookup 'CC))
+                   (define out-dir (at-dir "wasm"))
+                   (mkdir-p out-dir)
+                   ;; Recipe-dep artifacts from the `wasm-deps` target.
+                   ;; Each file has one flag/path token per line; split on
+                   ;; newlines (not whitespace -- `-Wl,-u,sym` tokens must
+                   ;; stay intact, and zuo's no-separator `string-split`
+                   ;; returns the whole string as one token anyway) and drop
+                   ;; blank lines. The linkflags are in final -L-then-reverse-l
+                   ;; order (libffi included); uflags is empty unless a recipe
+                   ;; registers symbols (the draw stack does; libffi-only doesn't).
+                   (define (read-flag-lines f)
+                     (if (file-exists? f)
+                         (filter (lambda (s) (not (equal? s "")))
+                                 (map string-trim (string-split (file->string f) "\n")))
+                         '()))
+                   (define dep-linkflags
+                     (read-flag-lines (build-path wasm-deps-dir ".wasm-deps-linkflags.txt")))
+                   (define dep-uflags
+                     (read-flag-lines (build-path wasm-deps-dir "wasm_deps_uflags.txt")))
+                   (define (preload src dest) (list "--preload-file" (~a src "@" dest)))
+                   ;; Installed packages (PKGS) are NOT preloaded here. Both
+                   ;; surfaces ship the package tree (share/pkgs + links.rktd +
+                   ;; any in-tree /pkgs/<name> the links file points at) as a
+                   ;; SEPARATE file_packager artifact -- share.data/share.data.js,
+                   ;; built by the orchestrator's `pack-pkgs` step -- loaded at
+                   ;; runtime (browser: shell-worker.js importScripts; node:
+                   ;; node-load-share.js indirect-eval). That keeps the package
+                   ;; payload out of this (expensive) link so a package change
+                   ;; needn't relink. The links-file parse that finds in-tree
+                   ;; /pkgs roots now lives once, in the orchestrator
+                   ;; (build/stages.rkt `links-pkgs-roots`). See build-wasm.md
+                   ;; "Packages as a separate data file".
+                   ;; Inputs shared by both surfaces (node `scheme.*` and
+                   ;; browser `scheme-web.*`). wasm_shell_io.o is NOT here --
+                   ;; it is browser-only and appended in the browser link.
+                   (define common-objs
+                     (append (list main_em.o boot.o init_rktio.o)
+                             ;; WASM C primitives referenced by wasm_extras.inc
+                             ;; (http/canvas/dom) + the libc stubs the deps need.
+                             wasm-prim-objs
+                             pbchunk-objs
+                             (list libkernel.a liblz4.a (target->path librktio.a))))
+                   ;; Boot images + collects/etc, preloaded into the MEMFS of
+                   ;; BOTH surfaces. Packages are not here -- they ship as a
+                   ;; separate share.data (see the note above core-preloads'
+                   ;; absence of share/pkgs and "Packages as a separate data
+                   ;; file" in build-wasm.md).
+                   (define core-preloads
+                     (append (preload petite-chunked.boot "petite.boot")
+                             (preload scheme-chunked.boot "scheme.boot")
+                             (preload racket-chunked.boot "racket.boot")
+                             (preload (at-source "../../../collects") "/collects")
+                             (preload (at-source "../../../etc") "/etc")))
+                   ;; Built-in link JS glue (idbfs-init.js, shell-tty.js,
+                   ;; node-*.js) lives outside the tree, in the racket-wasm
+                   ;; orchestrator's runtime-glue/ dir, passed via the
+                   ;; RUNTIME_GLUE_DIR make var.
+                   (define runtime-glue
+                     (let ([dir (lookup 'RUNTIME_GLUE_DIR "")])
+                       (when (equal? dir "")
+                         (error (~a "wasm link: RUNTIME_GLUE_DIR is not set;"
+                                    " the racket-wasm orchestrator sets it --"
+                                    " for a manual `make wasm` pass"
+                                    " RUNTIME_GLUE_DIR=<racket-wasm>/runtime-glue")))
+                       dir))
+                   ;; App-supplied emcc link JS (build/app.rkt manifest fields
+                   ;; 'pre-js/'post-js/'extern-pre-js), passed as space-joined
+                   ;; absolute paths in the LINK_*_JS make vars and shell-split
+                   ;; here; each file becomes `<flag> <path>`. They are appended to
+                   ;; the targeted surface only (APP_TARGET), AFTER the built-in
+                   ;; glue below -- so an app's --pre-js runs after node-load-share
+                   ;; and an app's --extern-pre-js after node-locate-file.
+                   (define (link-js-args flag var)
+                     (apply append
+                            (map (lambda (f) (list flag f))
+                                 (shell->strings (lookup var "")))))
+                   (define app-link-args
+                     (append (link-js-args "--extern-pre-js" 'LINK_EXTERN_PRE_JS)
+                             (link-js-args "--pre-js" 'LINK_PRE_JS)
+                             (link-js-args "--post-js" 'LINK_POST_JS)))
+                   (define app-target (lookup 'APP_TARGET "browser"))
+                   ;; The node (`scheme.*`) link: REPL over Node's stdio.
+                   (define node-args
+                     (append
+                      (list emcc "-O2" "-pthread" "-sUSE_ZLIB=1"
+                            "-o" (build-path out-dir "scheme.html"))
+                      common-objs
+                      ;; Recipe-dep -L/-l flags (libffi + any WASM_DEPS),
+                      ;; in final link order.
+                      dep-linkflags
+                      ;; node-locate-file.js (extern-pre-js): resolve
+                      ;; scheme.data relative to the script dir, not the
+                      ;; process CWD, so `node path/to/scheme.js` works from
+                      ;; anywhere. Must be extern-pre-js -- the --preload-file
+                      ;; loader runs at parse time, before ordinary --pre-js.
+                      (list "--extern-pre-js" (build-path runtime-glue "node-locate-file.js")
+                            "--post-js"       (build-path runtime-glue "node-tty.js")
+                            ;; node-load-share.js (pre-js): at runtime load the
+                            ;; separate package data file (share.data via
+                            ;; share.data.js) by indirect-eval, the node analog
+                            ;; of the worker's importScripts. Like the browser,
+                            ;; node ships packages OUT of the link so a package
+                            ;; change needn't relink scheme.*.
+                            "--pre-js"        (build-path runtime-glue "node-load-share.js"))
+                      ;; App link JS for the node surface only.
+                      (if (equal? app-target "node") app-link-args (list))
+                      ;; Core preloads only (boot/collects/etc); packages load
+                      ;; from share.data at runtime via node-load-share.js.
+                      core-preloads
+                      (list "-sEXIT_RUNTIME=1" "-sALLOW_MEMORY_GROWTH=1" "-sALLOW_TABLE_GROWTH=1"
+                            "-sEXPORTED_FUNCTIONS=_malloc,_free,_main,_setThrew,_memcpy,_memset"
+                            (~a "-sEXPORTED_RUNTIME_METHODS="
+                                "getValue,setValue,UTF8ToString,stringToUTF8,addFunction,removeFunction,"
+                                ;; Reached by name from the externally-loaded
+                                ;; share.data.js package loader (see browser link).
+                                "addRunDependency,removeRunDependency,FS_createPath,FS_createDataFile"))
+                      ;; Force-link the symbols the deps manifest registers
+                      ;; via Sforeign_symbol (wasm-ld would otherwise strip
+                      ;; them before init_foreign takes their address).
+                      ;; Empty for the libffi-only default.
+                      dep-uflags))
+                   ;; The browser (`scheme-web.*`) link: drives stdin/stdout and
+                   ;; the DOM over SharedArrayBuffers from a worker, with IDBFS
+                   ;; persistence. Adds wasm_shell_io.o, the idbfs/shell-tty JS,
+                   ;; the SAB/DOM exports, and FORCE_FILESYSTEM + idbfs.js. The
+                   ;; EXPORTED_FUNCTIONS list must match the addresses
+                   ;; shell-worker.js / shell-tty.js read out of the module.
+                   (define browser-args
+                     (append
+                      (list emcc "-O2" "-pthread" "-sUSE_ZLIB=1"
+                            "-o" (build-path out-dir "scheme-web.html"))
+                      common-objs
+                      (list wasm_shell_io.o)
+                      dep-linkflags
+                      (list "--pre-js"  (build-path runtime-glue "idbfs-init.js")
+                            "--post-js" (build-path runtime-glue "shell-tty.js"))
+                      ;; App link JS for the browser surface only.
+                      (if (equal? app-target "browser") app-link-args (list))
+                      ;; Core preloads only: the package tree ships as a
+                      ;; separate share.data/share.data.js (built by the
+                      ;; orchestrator's `pack-pkgs`, loaded by shell-worker.js)
+                      ;; so changing PKGS needn't relink this surface.
+                      core-preloads
+                      ;; Cache the preloaded .data package in IndexedDB keyed by
+                      ;; package name + size so a returning browser reuses it
+                      ;; instead of re-downloading the (large) boot/collects
+                      ;; payload on every load. Browser surface only -- the node
+                      ;; link has no persistent IDB to cache into. (Caches only
+                      ;; this core payload; the separate share.data is small and
+                      ;; uncached -- see pack-pkgs / build-wasm.md.)
+                      (list "--use-preload-cache")
+                      (list "-sEXIT_RUNTIME=1" "-sALLOW_MEMORY_GROWTH=1" "-sALLOW_TABLE_GROWTH=1"
+                            (~a "-sEXPORTED_FUNCTIONS="
+                                "_malloc,_free,_main,_setThrew,_memcpy,_memset,"
+                                "_shell_in_addr,_shell_in_cap,_shell_out_addr,_shell_out_cap,_shell_io_state_addr,"
+                                "_wasm_dom_cmd_seq_addr,_wasm_dom_cmd_len_addr,_wasm_dom_cmd_buf_addr,_wasm_dom_cmd_cap,"
+                                "_wasm_dom_reply_seq_addr,_wasm_dom_reply_len_addr,_wasm_dom_reply_buf_addr,_wasm_dom_reply_cap")
+                            (~a "-sEXPORTED_RUNTIME_METHODS="
+                                "getValue,setValue,UTF8ToString,stringToUTF8,addFunction,removeFunction,"
+                                "HEAPU8,HEAP32,FS,addRunDependency,removeRunDependency,"
+                                ;; Reached by name from the externally-loaded
+                                ;; share.data.js package loader (the in-link
+                                ;; loader uses minified locals; a separate
+                                ;; script needs them exported on Module).
+                                "FS_createPath,FS_createDataFile")
+                            "-sFORCE_FILESYSTEM=1" "-lidbfs.js")
+                      dep-uflags))
+                   ;; The link emits ONLY the runtime (scheme*.{js,wasm,data})
+                   ;; plus the glue baked in above (node-*.js, idbfs-init.js,
+                   ;; shell-tty.js). Surface assets (ide.*) and host-side glue
+                   ;; (shell-worker.js, serve.rkt) are no longer staged here --
+                   ;; the orchestrator (build/stages.rkt) copies them from the
+                   ;; repo so a surface can be swapped without touching the link.
+                   (shell/wait (map string->shell node-args))
+                   (shell/wait (map string->shell browser-args)))
+                :recur]
+
        [:target ,racket.boot (,(find-target "racket.so" cs-targets))
                 ,(lambda (path token)
                    (scheme "--script" (at-source "convert-to-boot.ss")
@@ -765,7 +1148,11 @@
                                  token))])]
            [(or (not (equal? (lookup 'BOOTFILE_RACKET) ""))
                 (not (equal? (lookup 'RACKET) "")))
-            ;; use `BOOTFILE_RACKET` or `RACKET` to build boot files
+            ;; use `BOOTFILE_RACKET` or `RACKET` to build boot files.
+            ;; `rktboot` produces target boot files but not the cross-compiler
+            ;; xpatch, so a real cross build (target machine != host) must use
+            ;; the `--enable-scheme`/`--enable-racket=auto` route, which runs
+            ;; `bootquick --xpatch`.
             (when xpatch?
               (error "cross build needs --enable-scheme=SCHEME or --enable-racket=auto"))
             `([:target bootfiles ()
@@ -878,7 +1265,19 @@
                 ,(lambda (token)
                    (install #f)
                    (setup))]
-       
+
+       ;; WASM cross setup: assemble the cross-compiler xpatch and run the
+       ;; cross `raco pkg install` + `raco setup` (via the host RACKET as
+       ;; --cross-server), compiling collections to compiled/<m>. Unlike
+       ;; `install-again`, this does not run the native `install` (which
+       ;; would try to copy the absent `racketcs`).
+       [:target wasm-setup ()
+                ,(lambda (token)
+                   (unless emscripten?
+                     (error "wasm-setup requires an Emscripten cross build"))
+                   (assemble-cross-xpatch)
+                   (wasm-setup))]
+
        [:target plain-install ()
                 ,(lambda (token)
                    (install #f))]
@@ -1092,32 +1491,39 @@
                  (list petite-v.boot scheme-v.boot racket-v.boot)
                  (list "petite.boot" "scheme.boot" "racket.boot")))])
 
-       (when xpatch?
-         ;; These two files don't get installed anywhere, because they're
-         ;; not part of a distribution, but a distro build expects them
-         ;; to be created in the directory where Racket was cross-built
-         (define compile-xpatch (at-dir (~a "compile-xpatch." m)))
-         (define library-xpatch (at-dir (~a "library-xpatch." m)))
-         (scheme/options (hash 'dir (at-dir "."))
-                         (list "--script"
-                               (find-relative-path (at-dir ".") (at-source "mk-cross-serve.ss"))
-                               (find-relative-path (at-dir ".") (at-source "."))
-                               "cross-serve.ss"
-                               (find-relative-path (at-dir ".") (at-source "../expander/env.ss"))))
-         (display-to-file (~a (file->string (at-dir "cross-serve.so"))
-                              (file->string xpatch))
-                          compile-xpatch
-                          :truncate)
-         (display-to-file (apply ~a
-                                 (map (lambda (name)
-                                        (file->string (at-dir (~a name "." host-m))))
-                                      library-xpatch-names))
-                          library-xpatch
-                          :truncate))
+       (assemble-cross-xpatch)
 
        (install-license-files sharepltdir))))
 
-  (define (setup)
+  ;; Assemble the cross-compiler xpatch files that `--cross-compiler`
+  ;; consumes: `cross-serve.so` plus `compile-xpatch.<m>` and
+  ;; `library-xpatch.<m>` in the build dir. Used by the native `install`
+  ;; and by the `wasm-setup` target (no `racketcs` needed).
+  (define (assemble-cross-xpatch)
+    (when xpatch?
+      ;; These two files don't get installed anywhere, because they're
+      ;; not part of a distribution, but a distro build expects them
+      ;; to be created in the directory where Racket was cross-built
+      (define compile-xpatch (at-dir (~a "compile-xpatch." m)))
+      (define library-xpatch (at-dir (~a "library-xpatch." m)))
+      (scheme/options (hash 'dir (at-dir "."))
+                      (list "--script"
+                            (find-relative-path (at-dir ".") (at-source "mk-cross-serve.ss"))
+                            (find-relative-path (at-dir ".") (at-source "."))
+                            "cross-serve.ss"
+                            (find-relative-path (at-dir ".") (at-source "../expander/env.ss"))))
+      (display-to-file (~a (file->string (at-dir "cross-serve.so"))
+                           (file->string xpatch))
+                       compile-xpatch
+                       :truncate)
+      (display-to-file (apply ~a
+                              (map (lambda (name)
+                                     (file->string (at-dir (~a name "." host-m))))
+                                   library-xpatch-names))
+                       library-xpatch
+                       :truncate)))
+
+  (define (cross-setup cross-compiler-args)
     (call-with-dest-racket
      (lambda (bindir dest-racket)
        (define copytree-racket (and cross? (hash-ref (config-bootstrap-racket) 'racket)))
@@ -1125,13 +1531,37 @@
                                    (hash-set config 'copytree-prepared-setup-files #t)
                                    config))
        (maybe-copytree copytree-config dest-racket copytree-racket at-dir)
-       (run-raco-setup config dest-racket copytree-racket
-                       ;; this can be redundant if it's also supplied via `SETUP_MACHINE_FLAGS`,
-                       ;; but redundant should be ok:
-                       (list "-MCR" (~a (path->complete-path (at-dir "compiled")) ":")
-                             "--cross-compiler" m (at-dir ".")))
+       (run-raco-setup config dest-racket copytree-racket cross-compiler-args)
        (maybe-libzo-move config dest-racket copytree-racket at-dir)
        (maybe-destdir-fix config dest-racket copytree-racket at-dir)
        (maybe-set-install-scope config dest-racket copytree-racket at-dir))))
 
+  (define (setup)
+    ;; `-MCR` = `-M -C -R`: machine-INDEPENDENT (`-M`) cross output, the
+    ;; standard portable cross-distribution form.
+    (cross-setup (list "-MCR" (~a (path->complete-path (at-dir "compiled")) ":")
+                       "--cross-compiler" m (at-dir "."))))
+
+  ;; Machine-SPECIFIC cross setup for the WASM build. Producing
+  ;; `compiled/<m>/*.zo` (target form, in place under `collects`) needs the
+  ;; cross-multi-compile mode -- i.e. `-M` + multiple `-R` roots (what the
+  ;; `setup` function's `-MCR` already does), *not* `--compile-machine`
+  ;; (which sets a non-#f compile-target-machine and disables cross-multi).
+  ;; The target form is only emitted when `cross-system-type` reports the
+  ;; target machine, which comes from the installed `system.rktd`; the
+  ;; surgical path skips the native install, so generate that file here
+  ;; (as `install` does via `gen-system.rkt`). Without it the cross-multi
+  ;; mode produces only the portable machine-independent `compiled/*.zo`.
+  (define (wasm-setup)
+    (define libpltdir (at-destdir config (shell-subst (lookup 'libpltdir) config)))
+    (mkdir-p libpltdir)
+    (install-racket (at-source "gen-system.rkt")
+                    (build-path libpltdir (~a (cs-suffix "system") ".rktd"))
+                    m (lookup 'KERNEL_TARGET_MACH m)
+                    (lookup 'CROSS_COMPILE_TARGET_KIND "machine")
+                    (at-source ".")
+                    (lookup 'PLT_CS_SLSP_SUFFIX)
+                    (if mac? "macosx" "other"))
+    (setup))
+
   the-targets)

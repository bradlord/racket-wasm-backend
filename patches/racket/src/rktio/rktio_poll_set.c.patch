diff --git a/racket/src/rktio/rktio_poll_set.c b/racket/src/rktio/rktio_poll_set.c
index efb7148982..e92017600e 100644
--- a/racket/src/rktio/rktio_poll_set.c
+++ b/racket/src/rktio/rktio_poll_set.c
@@ -341,6 +341,8 @@ static rktio_poll_set_t *alloc_fdset_arrays(rktio_t *rktio)
   if (!dynamic_fd_size) {
 # ifdef USE_ULIMIT
     dynamic_fd_size = ulimit(4, 0);
+# elif defined(RKTIO_USE_SYSCONF_FOR_FD_LIMIT)
+    dynamic_fd_size = sysconf(_SC_OPEN_MAX);
 # else
     dynamic_fd_size = getdtablesize();
 # endif
@@ -1038,6 +1040,8 @@ int rktio_get_fd_limit(rktio_poll_set_t *fds)
   actual_limit = ulimit(4, 0);
 #elif defined(FIXED_FD_LIMIT)
   actual_limit = FIXED_FD_LIMIT;
+#elif defined(RKTIO_USE_SYSCONF_FOR_FD_LIMIT)
+  actual_limit = sysconf(_SC_OPEN_MAX);
 #else
   actual_limit = getdtablesize();
 # endif

- Build the same-version host racket automatically. (RACKET=auto)

- Investigate pre-compiling fewer dependencies to tpb32l
    - Start up time vs size
    - We'll probably want to include (most of?) the core collections.
    - Maybe specify which packages to pre-compile?
    - Tried shipping app packages as source only (no .zo) on branch `experiment/pkg-source-uncompiled` (`build --pkg-source`). Compile-on-load was too slow to be workable; not merged.

- Investigate caching .zo files in IndexedDB.
    - We'll need to work out the issues with the browser event loop being blocked when wating for stdin. Maybe proxy-to-pthread already fixed this?

- Investigate dynamic library loading.
  - https://emscripten.org/docs/compiling/Dynamic-Linking.html
  - Can we get it to work at all?
  - Effect on size?

- node.js runtime improvements
    - Optional websocket -> socket proxy
    - Optional host filesystem access (via some sort of proxy?)

- Prevent web-repl from importing draw-lib, pict-lib, net-lib. If we need to dow

- Investigate switching from the app.rkt format to use using a `#lang info` info.rkt with some custom-defined fields
    - Then we'd really just be pointing to a module to package.

- Investigate a separate build for a main thread version vs the worker thread version.

- ~Confirm whether the libffi 3.5.x dep is really necessary or if we can somehow ignore deprecation warning with the bundle libffi~
  - "3.5.x is required; 3.4.x uses deprecated Emscripten JS-library names (generateFuncType, uleb128Encode) that newer emsdk renames."
  - It is required.

- Allow pointing to a built pkg catalog instead of rebuilding/caching it

- Host on github pages. Use service worker to add COOP/COEP headers?

- I think we end up cross-compiling draw-lib/gui-lib (plus anything depending on them) multiple times, since we install from the catalog before applying patches and reinstalling. Can we adjust consume.rkt to fix that?

- Harden consume.rkt's run-install benign-error tolerance (~line 301). The
  "packages installed, although setup reported errors" regex is meant for the
  benign rhombus .rhm launcher-template case, but it ALSO swallows a real
  web-repl compile error and silently ships share.data without print.zo/main.zo.
  Make "error: during making for <pkg>" fatal while still tolerating the
  launcher-template case. (Surfaced while fixing the pict border clipping.)

- Come up with a more systematic way of fixing function signature mismatches before they happen.
    - and/or improve error messages to make finding/fixing these easier when they occur. 
        - emcc flags: (-gsource-map --source-map-base <base url>) or maybe just -g
